# Pinned versions for the risk-engine training pipeline.
# TensorFlow is heavy (~500 MB) — only install when you're ready to
# train the real model. Everything else is small.
#
#   pip install -r tools/requirements.txt
#
# Python 3.10+
tensorflow>=2.15,<2.17
numpy>=1.26
tflite-support>=0.4.4
soundfile>=0.12
scikit-learn>=1.3
matplotlib>=3.8
tqdm>=4.66
ai-edge-litert>=2.0
